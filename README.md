# CL-basic

A prototype for a basic OpenCL host and kernel code

#### To compile:

+ To compile
```sh
$ cd build
$ cmake ../.
$ make
```

#### To test:
+ To run program
```sh
$ cd build
$ ./CL-basic
```
